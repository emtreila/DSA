# ✅ ADT Set — Boolean Array Representation

This project implements a **Set Abstract Data Type (ADT)** using a **dynamic array of Boolean values**, designed to efficiently represent sets of integers with bounded values.

---